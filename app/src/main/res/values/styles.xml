<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.Background">
        <item name="android:background">#23234d</item>
    </style>
    <style name="AppTheme.Button">
        <item name="android:background">#636265</item>
        <item name="android:fontFamily">roboto</item>
        <item name="android:textColor">#ffffff</item>
        <item name="android:layout_width">165dp</item>
    </style>
    <style name="AppTheme.SubmitButton">
        <item name="android:background">#accb12</item>
        <item name="android:fontFamily">roboto</item>
        <item name="android:textColor">#ffffff</item>
        <item name="android:layout_width">165dp</item>
    </style>
    <style name="AppTheme.Text">
        <item name="android:fontFamily">roboto</item>
        <item name="android:textColor">#ffffff</item>
        <item name="android:textStyle">bold</item>
    </style>
    <style name="AppTheme.EditText">
        <item name="android:background">@drawable/rounded_edittext</item>
        <item name="android:textAlignment">center</item>
    </style>
</resources>
